export interface Origin {
  location: Location;
  description: string;
}
export interface Location {
  lat: number;
  lng: number;
}

export interface Destination {
  location: Location;
  description: string;
}
export interface Location {
  lat: number;
  lng: number;
}
